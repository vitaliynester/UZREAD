import 'package:flutter/material.dart';

const int tabCount = 3;

const Color iconColor = Color(0xFF8ECDD0);
const Color backgroudColor = Color(0xFFEEF0F2);
const Color textColor = Color(0xFF0D7E83);

const String serverIP = "";
const int serverPort = 0;

enum MyException { NoInternet, NoResult, ServerException }
